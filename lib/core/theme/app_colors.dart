import 'package:flutter/material.dart';

class AppColors {
  // Primary — Deep Teal (peace, health, Islam)
  static const primary = Color(0xFF0F6E56);
  static const onPrimary = Colors.white;
  static const primaryContainer = Color(0xFFD0F1E5);
  static const onPrimaryContainer = Color(0xFF002118);

  // Secondary — Islamic Gold
  static const secondary = Color(0xFFBA7517);
  static const onSecondary = Colors.white;
  static const secondaryContainer = Color(0xFFFFDDB1);
  static const onSecondaryContainer = Color(0xFF2B1700);

  // Tertiary - A soft Sage or complementary color
  static const tertiary = Color(0xFF506352);
  static const onTertiary = Colors.white;

  // Backgrounds & Surfaces (Light)
  static const backgroundLight = Color(0xFFFDFDF5);
  static const onBackgroundLight = Color(0xFF191C1A);
  static const surfaceLight = Color(0xFFFDFDF5);
  static const onSurfaceLight = Color(0xFF191C1A);
  static const surfaceVariantLight = Color(0xFFDBE5DE);
  static const onSurfaceVariantLight = Color(0xFF404944);

  // Backgrounds & Surfaces (Dark)
  static const backgroundDark = Color(0xFF0D1B2A);
  static const onBackgroundDark = Color(0xFFE1E3DF);
  static const surfaceDark = Color(0xFF152535);
  static const onSurfaceDark = Color(0xFFE1E3DF);
  static const surfaceVariantDark = Color(0xFF404944);
  static const onSurfaceVariantDark = Color(0xFFBFC9C2);

  // Risk colors
  static const error = Color(0xFFBA1A1A);
  static const onError = Colors.white;
  static const success = Color(0xFF1D9E75);
  static const warning = Color(0xFFF0A500);

  // Helper colors for specific cards
  static const prayerCardLight = Color(0xFFFFFFFF);
  static const prayerCardDark = Color(0xFF1E2F40);
}
